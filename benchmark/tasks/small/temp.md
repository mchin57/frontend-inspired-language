# Temperature converter

Build a UI with a text input for a temperature in Celsius and a live-updating
line showing the Fahrenheit equivalent (integer math is fine).

Acceptance:
- Typing `20` shows `68` as Fahrenheit somewhere on the page.
- Non-numeric input is treated as 0.
- The display updates on every keystroke without pressing any button.
