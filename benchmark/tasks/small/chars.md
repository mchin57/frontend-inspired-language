# Character counter

Build a UI with a text input and a live counter line "N/100". When the text is
longer than 100 characters, also show a warning message "Too long".

Acceptance:
- Counter updates on every keystroke.
- Warning appears only when length > 100 and disappears when shortened.
